---
title: MediaEncryptedEvent.initDataType
slug: Web/API/MediaEncryptedEvent/initDataType
page-type: web-api-instance-property
browser-compat: api.MediaEncryptedEvent.initDataType
---

{{APIRef("Encrypted Media Extensions")}}{{SecureContext_Header}}

Thuộc tính chỉ đọc **`initDataType`** của giao diện {{domxref("MediaEncryptedEvent")}} trả về chuỗi (không phân biệt chữ hoa chữ thường) với loại định dạng của dữ liệu khởi tạo liên quan với sự kiện `encrypted`.

## Giá trị

Chuỗi (không phân biệt chữ hoa chữ thường) với loại định dạng.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

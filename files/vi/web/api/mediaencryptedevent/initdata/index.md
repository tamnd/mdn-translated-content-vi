---
title: MediaEncryptedEvent.initData
slug: Web/API/MediaEncryptedEvent/initData
page-type: web-api-instance-property
browser-compat: api.MediaEncryptedEvent.initData
---

{{APIRef("Encrypted Media Extensions")}}{{SecureContext_Header}}

Thuộc tính chỉ đọc **`initData`** của giao diện {{domxref("MediaEncryptedEvent")}} trả về {{jsxref("ArrayBuffer")}} chứa dữ liệu khởi tạo liên quan đến sự kiện `encrypted`. Nếu không có dữ liệu khởi tạo liên quan đến định dạng cụ thể, nó là `null`.

## Giá trị

{{jsxref("ArrayBuffer")}} hoặc `null`.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

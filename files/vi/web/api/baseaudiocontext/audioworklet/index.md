---
title: "BaseAudioContext: thuộc tính audioWorklet"
short-title: audioWorklet
slug: Web/API/BaseAudioContext/audioWorklet
page-type: web-api-instance-property
browser-compat: api.BaseAudioContext.audioWorklet
---

{{ APIRef("Web Audio API") }}{{securecontext_header}}

Thuộc tính chỉ đọc `audioWorklet` của giao diện
{{domxref("BaseAudioContext")}} trả về một thể hiện của
{{domxref("AudioWorklet")}} có thể được dùng để thêm các lớp dẫn xuất từ
{{domxref("AudioWorkletProcessor")}} nhằm triển khai xử lý âm thanh tùy chỉnh.

## Giá trị

Một thể hiện {{domxref("AudioWorklet")}}.

## Ví dụ

_Để xem ví dụ đầy đủ minh họa xử lý âm thanh do người dùng tự định nghĩa, hãy xem trang
{{domxref("AudioWorkletNode")}}._

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích với trình duyệt

{{Compat}}

## Xem thêm

- [Sử dụng Web Audio API](/en-US/docs/Web/API/Web_Audio_API/Using_Web_Audio_API)
- {{domxref("AudioWorkletNode")}}

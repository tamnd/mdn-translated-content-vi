---
title: "MediaKeyStatusMap: phương thức values()"
short-title: values()
slug: Web/API/MediaKeyStatusMap/values
page-type: web-api-instance-method
browser-compat: api.MediaKeyStatusMap.values
---

{{APIRef("Encrypted Media Extensions")}}

Phương thức **`values()`** của giao diện {{domxref("MediaKeyStatusMap")}} trả về một đối tượng Iterator mới, chứa các giá trị cho mỗi phần tử trong bản đồ trạng thái, theo thứ tự chèn.

## Cú pháp

```js-nolint
values()
```

### Tham số

Không có.

### Giá trị trả về

Một iterator mới.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

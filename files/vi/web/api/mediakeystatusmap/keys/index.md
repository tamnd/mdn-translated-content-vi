---
title: "MediaKeyStatusMap: phương thức keys()"
short-title: keys()
slug: Web/API/MediaKeyStatusMap/keys
page-type: web-api-instance-method
browser-compat: api.MediaKeyStatusMap.keys
---

{{APIRef("Encrypted Media Extensions")}}

Phương thức **`keys()`** của giao diện {{domxref("MediaKeyStatusMap")}} trả về một đối tượng Iterator mới, chứa các khóa cho mỗi phần tử trong bản đồ trạng thái, theo thứ tự chèn.

## Cú pháp

```js-nolint
keys()
```

### Tham số

Không có.

### Giá trị trả về

Một iterator mới.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

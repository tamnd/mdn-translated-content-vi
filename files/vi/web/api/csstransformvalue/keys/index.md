---
title: "CSSTransformValue: keys() method"
short-title: keys()
slug: Web/API/CSSTransformValue/keys
page-type: web-api-instance-method
browser-compat: api.CSSTransformValue.keys
---

{{APIRef("CSS Typed OM")}}

Phương thức **`CSSTransformValue.keys()`** trả về một đối tượng _array iterator_ mới chứa các khóa cho mỗi chỉ mục trong mảng.

## Cú pháp

```js-nolint
keys()
```

### Tham số

Không có.

### Giá trị trả về

Một {{jsxref("Array")}} mới.

## Ví dụ

Sẽ bổ sung

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

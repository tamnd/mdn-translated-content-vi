---
title: "BluetoothRemoteGATTCharacteristic: phương thức startNotifications()"
short-title: startNotifications()
slug: Web/API/BluetoothRemoteGATTCharacteristic/startNotifications
page-type: web-api-instance-method
status:
  - experimental
browser-compat: api.BluetoothRemoteGATTCharacteristic.startNotifications
---

{{APIRef("Bluetooth API")}}{{SeeCompatTable}}{{SecureContext_Header}}

Phương thức **`BluetoothRemoteGATTCharacteristic.startNotifications()`** trả về một {{jsxref("Promise")}} tới thực thể `BluetoothRemoteGATTCharacteristic` khi có một thông báo đang hoạt động trên nó.

## Cú pháp

```js-nolint
startNotifications()
```

### Tham số

Không có.

### Giá trị trả về

Một {{jsxref("Promise")}} tới thực thể `BluetoothRemoteGATTCharacteristic`.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

---
title: "CSSScale: z property"
short-title: z
slug: Web/API/CSSScale/z
page-type: web-api-instance-property
browser-compat: api.CSSScale.z
---

{{APIRef("CSS Typed OM")}}

Thuộc tính **`z`** của giao diện {{domxref("CSSScale")}} đại diện cho thành phần z của vectơ tịnh tiến. Giá trị dương di chuyển phần tử về phía người xem, giá trị âm di chuyển xa hơn.

Nếu giá trị này có mặt thì phép biến đổi là phép biến đổi 3D và thuộc tính `is2D` sẽ được đặt thành false.

## Giá trị

Một {{domxref("CSSNumericValue")}}. Có thể được đặt bằng một số, số đó sẽ được bọc trong {{domxref("CSSUnitValue")}} với `unit: "number"`.

## Ví dụ

Sẽ cập nhật sau.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

---
title: "SyncEvent: thuộc tính tag"
short-title: tag
slug: Web/API/SyncEvent/tag
page-type: web-api-instance-property
browser-compat: api.SyncEvent.tag
---

{{APIRef("Background Sync")}}{{AvailableInWorkers("service")}}

Thuộc tính chỉ đọc **`tag`** của giao diện {{domxref("SyncEvent")}} trả về mã định danh do nhà phát triển định nghĩa cho `SyncEvent` này. Đây là giá trị được truyền trong tham số `tag` của hàm khởi tạo {{domxref("SyncEvent.SyncEvent","SyncEvent()")}}.

## Giá trị

Mã định danh do nhà phát triển định nghĩa cho `SyncEvent` này.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

---
title: "RTCPeerConnection: phương thức getSenders()"
short-title: getSenders()
slug: Web/API/RTCPeerConnection/getSenders
page-type: web-api-instance-method
browser-compat: api.RTCPeerConnection.getSenders
---

{{APIRef("WebRTC")}}

Phương thức **`getSenders()`** của giao diện {{domxref("RTCPeerConnection")}} trả về một mảng các đối tượng {{domxref("RTCRtpSender")}}, mỗi đối tượng biểu diễn bộ gửi {{Glossary("RTP")}} chịu trách nhiệm truyền dữ liệu của một track.

## Cú pháp

```js-nolint
getSenders()
```

### Tham số

Không có.

### Giá trị trả về

Một mảng các đối tượng {{domxref("RTCRtpSender")}}.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

---
title: "MediaKeySession: phương thức remove()"
short-title: remove()
slug: Web/API/MediaKeySession/remove
page-type: web-api-instance-method
browser-compat: api.MediaKeySession.remove
---

{{APIRef("Encrypted Media Extensions")}}{{SecureContext_Header}}

Phương thức `remove()` của giao diện {{domxref('MediaKeySession')}} trả về một {{jsxref('Promise')}} sau khi xóa mọi dữ liệu phiên liên quan đến đối tượng hiện tại.

## Cú pháp

```js-nolint
remove()
```

### Tham số

Không có.

### Giá trị trả về

Một {{jsxref('Promise')}} phân giải thành {{jsxref("undefined")}}.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

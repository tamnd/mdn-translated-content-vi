---
title: "AudioData: thuộc tính duration"
short-title: duration
slug: Web/API/AudioData/duration
page-type: web-api-instance-property
browser-compat: api.AudioData.duration
---

{{APIRef("WebCodecs API")}}{{AvailableInWorkers("window_and_dedicated")}}

Thuộc tính chỉ đọc **`duration`** của giao diện {{domxref("AudioData")}} trả về thời lượng của đối tượng `AudioData` này tính bằng micro giây.

## Giá trị

Một số nguyên.

## Ví dụ

Ví dụ dưới đây in giá trị của `duration` ra console.

```js
console.log(AudioData.duration);
```

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích với trình duyệt

{{Compat}}

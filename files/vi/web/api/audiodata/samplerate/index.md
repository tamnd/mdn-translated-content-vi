---
title: "AudioData: thuộc tính sampleRate"
short-title: sampleRate
slug: Web/API/AudioData/sampleRate
page-type: web-api-instance-property
browser-compat: api.AudioData.sampleRate
---

{{APIRef("WebCodecs API")}}{{AvailableInWorkers("window_and_dedicated")}}

Thuộc tính chỉ đọc **`sampleRate`** của giao diện {{domxref("AudioData")}} trả về tốc độ lấy mẫu theo Hz.

## Giá trị

Một giá trị thập phân.

## Ví dụ

Ví dụ dưới đây in giá trị của `sampleRate` ra console.

```js
console.log(audioData.sampleRate);
```

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích với trình duyệt

{{Compat}}

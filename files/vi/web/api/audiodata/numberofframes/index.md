---
title: "AudioData: thuộc tính numberOfFrames"
short-title: numberOfFrames
slug: Web/API/AudioData/numberOfFrames
page-type: web-api-instance-property
browser-compat: api.AudioData.numberOfFrames
---

{{APIRef("WebCodecs API")}}{{AvailableInWorkers("window_and_dedicated")}}

Thuộc tính chỉ đọc **`numberOfFrames`** của giao diện {{domxref("AudioData")}} trả về số lượng frame trong đối tượng `AudioData`.

## Giá trị

Một số nguyên.

## Ví dụ

Ví dụ dưới đây in giá trị của `numberOfFrames` ra console.

```js
console.log(AudioData.numberOfFrames);
```

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích với trình duyệt

{{Compat}}

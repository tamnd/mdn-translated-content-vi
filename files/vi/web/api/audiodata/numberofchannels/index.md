---
title: "AudioData: thuộc tính numberOfChannels"
short-title: numberOfChannels
slug: Web/API/AudioData/numberOfChannels
page-type: web-api-instance-property
browser-compat: api.AudioData.numberOfChannels
---

{{APIRef("WebCodecs API")}}{{AvailableInWorkers("window_and_dedicated")}}

Thuộc tính chỉ đọc **`numberOfChannels`** của giao diện {{domxref("AudioData")}} trả về số lượng kênh trong đối tượng `AudioData`.

## Giá trị

Một số nguyên.

## Ví dụ

Ví dụ dưới đây in giá trị của `numberOfChannels` ra console.

```js
console.log(AudioData.numberOfChannels);
```

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích với trình duyệt

{{Compat}}

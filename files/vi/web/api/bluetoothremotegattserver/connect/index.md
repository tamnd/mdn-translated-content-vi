---
title: "BluetoothRemoteGATTServer: phương thức connect()"
short-title: connect()
slug: Web/API/BluetoothRemoteGATTServer/connect
page-type: web-api-instance-method
status:
  - experimental
browser-compat: api.BluetoothRemoteGATTServer.connect
---

{{APIRef("Bluetooth API")}}{{SeeCompatTable}}{{SecureContext_Header}}

Phương thức **`BluetoothRemoteGATTServer.connect()`** khiến môi trường thực thi script kết nối tới `this.device`.

## Cú pháp

```js-nolint
connect()
```

### Tham số

Không có.

### Giá trị trả về

Một {{jsxref("Promise")}} được phân giải thành một {{domxref("BluetoothRemoteGATTServer")}}.

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích của trình duyệt

{{Compat}}

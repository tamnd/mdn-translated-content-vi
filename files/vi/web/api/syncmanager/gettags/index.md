---
title: "SyncManager: getTags() method"
short-title: getTags()
slug: Web/API/SyncManager/getTags
page-type: web-api-instance-method
browser-compat: api.SyncManager.getTags
---

{{APIRef("Background Sync")}}{{AvailableInWorkers}}

Phương thức **`getTags()`** của giao diện {{domxref("SyncManager")}} trả về danh sách các định danh do nhà phát triển xác định cho các đăng ký `SyncManager`.

## Cú pháp

```js-nolint
getTags()
```

### Tham số

Không có.

### Giá trị trả về

Một {{jsxref("Promise")}} phân giải thành một mảng chuỗi chứa các định danh do nhà phát triển xác định cho các đăng ký `SyncManager`.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

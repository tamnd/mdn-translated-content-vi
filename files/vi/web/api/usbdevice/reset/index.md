---
title: "USBDevice: phương thức reset()"
short-title: reset()
slug: Web/API/USBDevice/reset
page-type: web-api-instance-method
status:
  - experimental
browser-compat: api.USBDevice.reset
---

{{APIRef("WebUSB API")}}{{SeeCompatTable}}{{SecureContext_Header}}{{AvailableInWorkers}}

Phương thức **`reset()`** của giao diện {{domxref("USBDevice")}} trả về một {{jsxref("promise")}} được giải quyết khi thiết bị được đặt lại và tất cả các thao tác ứng dụng bị hủy bỏ cùng với các promise của chúng bị từ chối.

## Cú pháp

```js-nolint
reset()
```

### Tham số

Không có.

### Giá trị trả về

Một {{jsxref("promise")}}.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

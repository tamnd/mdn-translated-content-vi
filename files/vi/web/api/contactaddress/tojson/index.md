---
title: "Phương thức toJSON() của ContactAddress"
short-title: toJSON()
slug: Web/API/ContactAddress/toJSON
page-type: web-api-instance-method
status:
  - experimental
browser-compat: api.ContactAddress.toJSON
---

{{securecontext_header}}{{APIRef("Contact Picker API")}}{{SeeCompatTable}}

Phương thức **`toJSON()`** của giao diện {{domxref("ContactAddress")}} là bộ tuần tự hóa tiêu chuẩn trả về biểu diễn JSON của các thuộc tính của đối tượng `ContactAddress`.

## Cú pháp

```js-nolint
toJSON()
```

### Tham số

Không có.

### Giá trị trả về

Một đối tượng JSON.

## Thông số kỹ thuật

{{Specifications}}

## Khả năng tương thích của trình duyệt

{{Compat}}

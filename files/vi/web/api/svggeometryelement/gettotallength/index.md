---
title: "SVGGeometryElement: phương thức getTotalLength()"
short-title: getTotalLength()
slug: Web/API/SVGGeometryElement/getTotalLength
page-type: web-api-instance-method
browser-compat: api.SVGGeometryElement.getTotalLength
---

{{APIRef("SVG")}}

Phương thức **`SVGGeometryElement.getTotalLength()`** trả về giá trị tính toán của user agent cho tổng độ dài của đường dẫn tính bằng đơn vị người dùng.

## Cú pháp

```js-nolint
getTotalLength()
```

### Tham số

Không có.

### Giá trị trả về

Một số thực cho biết tổng độ dài của đường dẫn tính bằng đơn vị người dùng.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

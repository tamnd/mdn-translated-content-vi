---
title: "FontFaceSet: phương thức values()"
short-title: values()
slug: Web/API/FontFaceSet/values
page-type: web-api-instance-method
browser-compat: api.FontFaceSet.values
---

{{APIRef("CSS Font Loading API")}}{{AvailableInWorkers}}

Phương thức **`values()`** của giao diện {{domxref("FontFaceSet")}} trả về đối tượng iterator mới chứa các giá trị của mỗi phần tử trong đối tượng `FontFaceSet` theo thứ tự chèn.

## Cú pháp

```js-nolint
values()
```

### Tham số

Không có.

### Giá trị trả về

Iterator mới chứa các giá trị của mỗi phần tử trong `FontFaceSet` đã cho, theo thứ tự chèn.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

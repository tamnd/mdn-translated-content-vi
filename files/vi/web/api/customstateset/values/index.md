---
title: "CustomStateSet: values() method"
short-title: values()
slug: Web/API/CustomStateSet/values
page-type: web-api-instance-method
browser-compat: api.CustomStateSet.values
---

{{APIRef("Web Components")}}

Phương thức **`values()`** của giao diện {{domxref("CustomStateSet")}} trả về một đối tượng iterator mới tạo ra các giá trị cho mỗi phần tử trong đối tượng `CustomStateSet` theo thứ tự chèn vào.

## Cú pháp

```js-nolint
values()
```

### Tham số

Không có.

### Giá trị trả về

Một đối tượng iterator mới chứa các giá trị cho mỗi phần tử trong `CustomStateSet` đã cho, theo thứ tự chèn vào.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}

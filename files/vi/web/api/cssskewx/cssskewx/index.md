---
title: "CSSSkewX: hàm khởi tạo CSSSkewX()"
short-title: CSSSkewX()
slug: Web/API/CSSSkewX/CSSSkewX
page-type: web-api-constructor
browser-compat: api.CSSSkewX.CSSSkewX
---

{{APIRef("CSS Typed OM")}}{{AvailableInWorkers}}

Hàm khởi tạo **`CSSSkewX()`** tạo một đối tượng {{domxref("CSSSkewX")}} mới biểu thị giá trị {{cssxref("transform-function/skewX", "skewX()")}} của thuộc tính {{CSSXRef('transform')}} riêng lẻ trong CSS.

## Cú pháp

```js-nolint
new CSSSkewX(ax)
```

### Tham số

- {{domxref('CSSSkewx.ax','ax')}}
  - : Một giá trị cho góc `ax` của đối tượng {{domxref('CSSSkewX')}} cần tạo. Đây phải là {{domxref('CSSNumericValue')}}.

## Ví dụ

Sẽ được thêm.

## Thông số kỹ thuật

{{Specifications}}

## Tương thích trình duyệt

{{Compat}}
